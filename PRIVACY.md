# 隐私说明

ds Eagle Tagger 在用户本机读取 Eagle 当前图库中的图片，并在本机运行 WD14 模型。图片、文件路径、预测标签和处理记录不会上传给作者。

插件不包含账号系统、广告、遥测或行为统计。

联网仅用于：

- 安装 Python 运行环境和 GPU 推理依赖；
- 下载 WD14 模型及维护者批准的模型目录；
- 用户主动点击检查模型更新。

用户也可以通过“导入本地模型”使用本地 `model.onnx` 与 `selected_tags.csv`，导入过程不会上传文件。

